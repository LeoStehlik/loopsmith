# Loopsmith Examples

Start with [`before-after-eval.md`](before-after-eval.md).

It shows how a recurring agent-quality problem becomes a baseline-vs-candidate eval, with review criteria and promotion evidence instead of taste arguments.

Fast path:

```bash
make test
sed -n '1,80p' examples/before-after-eval.md
```
